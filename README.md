# SaaS Visibility Guard

A SaaS visibility guard that detects security vulnerabilities and generates reports.

## Usage

1. Create an instance of `SaasVisibilityGuard`.
2. Add SaaS apps with their vulnerabilities using `add_saaas_app`.
3. Detect vulnerabilities using `detect_vulnerabilities`.
4. Generate reports using `generate_report`.

## Example
