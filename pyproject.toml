[tool.pytest.ini_options]
addopts = ["-v"]

[tool.poetry]
name = "saas-visibility-guard"
version = "1.0.0"
description = "A SaaS visibility guard"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^7.1"
